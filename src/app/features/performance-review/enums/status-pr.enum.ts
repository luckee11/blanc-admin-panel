/** Статусы цикла performance review (соответствует backend StatusPR). */
export enum StatusPR {
  Draft = 'draft',
  InProgress = 'in_progress',
  Completed = 'completed',
  Archived = 'archived',
}
